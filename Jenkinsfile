pipeline {
    agent { dockerfile true }
    stages {
        stage('Build') {
            steps {
                echo  'git clone https://github.com/meendyy/htmlteste.git'
            }
        }
    }
}