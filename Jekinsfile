pipeline {
    agent { dockerfile true }
    stages {
        stage('Build') {
                    reuseNode true
                }
            }
            steps {
                sh 'gradle --version'
            }
        }
    }
}

